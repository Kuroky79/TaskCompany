import React from 'react';
import {Switch, Route, Redirect} from "react-router-dom";


const AppRouter = () => {
    return (
        auth === true
            ?
            <Switch>

            </Switch>
            :
            <Switch>

            </Switch>
    );
};

export default AppRouter;
